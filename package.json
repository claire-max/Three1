{
  "name": "three1",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^4.3.0"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "three": "^0.151.3"
  }
}
